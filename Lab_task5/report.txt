task1
1. create configuration file
    Now I'm working as the CA
    1. copy openssl.cnf from usr/lib/ssl/openssl.cnf
    2. create subdirectories as per configuration
    3. generate self signed certificate for the ca
     openssl req -new -x509 -keyout ca.key -out ca.crt -config openssl.cnf


    **pem password: Lab5Task#

2. Creating a certificate for example.com
    now, I'm working as example.com
    1. generate key pair:  openssl genrsa -des3 -out server.key 1024

    pass phrase: rsaKeyPair#


    2. generate csr:  openssl req -new -key server.key -out server.csr -config openssl.cnf

    pass phrase for server.key: rsaKeyPair#
    challenge password: chal_pass#

    3. generate certificate:  openssl ca -in server.csr -out server.crt -cert ca.crt -keyfile ca.key -config openssl.cnf

    pass phrase for ca.key: Lab5Task#

3. Launching a simple web server with certificate generated

